services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite+aiosqlite:////app/data/liftiq.db
      ENVIRONMENT: production
    volumes:
      - liftiq-data:/app/data

volumes:
  liftiq-data:
